This is where the robot's control interface lies. It takes input from
the joysticks and uses it to control the robot.

It also contains the GUI displaying the camera feed and telemetry

Physically it is an atmel32u4 plugged into a normal linux PC. The atmel handles
input from physical buttons/joysticks and possibly sending data via whatevever
the comms system is
